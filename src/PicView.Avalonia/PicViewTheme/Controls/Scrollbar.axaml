<ResourceDictionary
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:ClassModifier="internal">
    <ControlTheme x:Key="{x:Type ScrollBar}" TargetType="ScrollBar">
        <Setter Property="Cursor" Value="Arrow" />

        <Style Selector="^:horizontal">
            <Setter Property="Height" Value="18" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <ControlTemplate>
                    <Border Background="{DynamicResource ScrollbarBackgroundColor}" UseLayoutRounding="False">
                        <Grid ColumnDefinitions="Auto,*,Auto">
                            <RepeatButton
                                Name="PART_LineUpButton"
                                Grid.Row="0"
                                Grid.Column="0"
                                Width="16"
                                Height="16"
                                MinWidth="30"
                                VerticalAlignment="Center"
                                Background="Transparent"
                                Classes="repeat"
                                Focusable="False"
                                Interval="55">
                                <Path
                                    Width="12"
                                    Height="12"
                                    Data="{StaticResource FilledChevronLeftGeometry}"
                                    Fill="{DynamicResource ScrollbarThumbColor}"
                                    Stretch="Fill" />
                            </RepeatButton>
                            <Track
                                Grid.Column="1"
                                DeferThumbDrag="{TemplateBinding ScrollViewer.IsDeferredScrollingEnabled}"
                                Maximum="{TemplateBinding Maximum}"
                                Minimum="{TemplateBinding Minimum}"
                                Orientation="{TemplateBinding Orientation}"
                                ViewportSize="{TemplateBinding ViewportSize}"
                                Value="{TemplateBinding Value,
                                                        Mode=TwoWay}">
                                <Track.DecreaseButton>
                                    <RepeatButton
                                        Name="PART_PageUpButton"
                                        Background="Transparent"
                                        Classes="repeattrack"
                                        Focusable="False"
                                        Interval="55" />
                                </Track.DecreaseButton>
                                <Track.IncreaseButton>
                                    <RepeatButton
                                        Name="PART_PageDownButton"
                                        Background="Transparent"
                                        Classes="repeattrack"
                                        Focusable="False"
                                        Interval="55" />
                                </Track.IncreaseButton>
                                <Thumb Name="thumb" />
                            </Track>
                            <RepeatButton
                                Name="PART_LineDownButton"
                                Grid.Column="2"
                                Width="16"
                                Height="16"
                                MinWidth="30"
                                VerticalAlignment="Center"
                                Background="Transparent"
                                Classes="repeat"
                                Focusable="False"
                                Interval="55">
                                <Path
                                    Width="12"
                                    Height="12"
                                    Data="{StaticResource FilledChevronRightGeometry}"
                                    Fill="{DynamicResource ScrollbarThumbColor}"
                                    Stretch="Fill" />
                            </RepeatButton>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter>
        </Style>

        <Style Selector="^:vertical">
            <Setter Property="Width" Value="30" />
            <Setter Property="Template">
                <ControlTemplate>
                    <Border
                        x:Name="Border"
                        Background="Transparent"
                        UseLayoutRounding="False">
                        <DockPanel>
                            <RepeatButton
                                Name="PART_LineUpButton"
                                MinHeight="25"
                                HorizontalAlignment="Center"
                                Background="Transparent"
                                Classes="repeat"
                                DockPanel.Dock="Top"
                                Focusable="False"
                                Interval="55">
                                <Path
                                    Width="16"
                                    Height="16"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Data="{StaticResource FilledArrowUpGeometry}"
                                    Fill="{DynamicResource ScrollbarThumbColor}"
                                    Stretch="Fill" />
                            </RepeatButton>

                            <RepeatButton
                                Name="PART_LineDownButton"
                                MinHeight="25"
                                HorizontalAlignment="Center"
                                Background="Transparent"
                                Classes="repeat"
                                DockPanel.Dock="Bottom"
                                Focusable="False"
                                Interval="55">
                                <Path
                                    Width="16"
                                    Height="16"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Data="{StaticResource FilledArrowDownGeometry}"
                                    Fill="{DynamicResource ScrollbarThumbColor}"
                                    Stretch="Fill" />
                            </RepeatButton>

                            <Track
                                DeferThumbDrag="{TemplateBinding ScrollViewer.IsDeferredScrollingEnabled}"
                                IsDirectionReversed="True"
                                Maximum="{TemplateBinding Maximum}"
                                Minimum="{TemplateBinding Minimum}"
                                Orientation="{TemplateBinding Orientation}"
                                ViewportSize="{TemplateBinding ViewportSize}"
                                Value="{TemplateBinding Value,
                                                        Mode=TwoWay}">
                                <Track.DecreaseButton>
                                    <RepeatButton
                                        Name="PART_PageUpButton"
                                        Background="Transparent"
                                        Classes="repeattrack"
                                        Focusable="False"
                                        Interval="55" />
                                </Track.DecreaseButton>
                                <Track.IncreaseButton>
                                    <RepeatButton
                                        Name="PART_PageDownButton"
                                        Background="Transparent"
                                        Classes="repeattrack"
                                        Focusable="False"
                                        Interval="55" />
                                </Track.IncreaseButton>
                                <Thumb Name="thumb" />
                            </Track>
                        </DockPanel>
                    </Border>
                </ControlTemplate>
            </Setter>
        </Style>

        <Style Selector="^ /template/ Thumb#thumb">
            <Setter Property="Background" Value="{DynamicResource ScrollbarThumbColor}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{DynamicResource ScrollbarThumbBorderColor}" />
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding CornerRadius}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style Selector="^ /template/ Thumb#thumb:pointerover">
            <Setter Property="Background" Value="{DynamicResource AccentColor}" />
        </Style>
        <Style Selector="^ /template/ Thumb#thumb:pressed">
            <Setter Property="Background" Value="{DynamicResource SecondaryAccentColor}" />
        </Style>
        <Style Selector="^:horizontal /template/ Thumb#thumb">
            <Setter Property="MinWidth" Value="{StaticResource ScrollBarThickness}" />
            <Setter Property="Height" Value="16" />
        </Style>
        <Style Selector="^:vertical /template/ Thumb#thumb">
            <Setter Property="MinHeight" Value="{DynamicResource ScrollBarThickness}" />
            <Setter Property="Width" Value="16" />
        </Style>
        <Style Selector="^ /template/ RepeatButton.repeat">
            <Setter Property="Padding" Value="2" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
        <Style Selector="^ /template/ RepeatButton.repeattrack">
            <Setter Property="Template">
                <ControlTemplate>
                    <Border Background="Transparent" />
                </ControlTemplate>
            </Setter>
        </Style>

        <Style Selector="^ /template/ RepeatButton:pointerover &gt; PathIcon">
            <Setter Property="Foreground" Value="{DynamicResource AccentColor}" />
        </Style>

    </ControlTheme>



    <ControlTheme x:Key="Floating" TargetType="ScrollBar">
        <Setter Property="Cursor" Value="Arrow" />

        <Style Selector="^:horizontal">
            <Setter Property="Template">
                <ControlTemplate>
                    <Track
                        DeferThumbDrag="{TemplateBinding ScrollViewer.IsDeferredScrollingEnabled}"
                        Maximum="{TemplateBinding Maximum}"
                        Minimum="{TemplateBinding Minimum}"
                        Orientation="{TemplateBinding Orientation}"
                        ViewportSize="{TemplateBinding ViewportSize}"
                        Value="{TemplateBinding Value,
                                                Mode=TwoWay}">
                        <Thumb Name="thumb" />
                    </Track>
                </ControlTemplate>
            </Setter>
        </Style>

        <Style Selector="^:vertical">
            <Setter Property="Template">
                <ControlTemplate>
                    <!--  Setting reversed direction to true fixes the scrollbar starting at the bottom  -->
                    <Track
                        DeferThumbDrag="{TemplateBinding ScrollViewer.IsDeferredScrollingEnabled}"
                        IsDirectionReversed="True"
                        Maximum="{TemplateBinding Maximum}"
                        Minimum="{TemplateBinding Minimum}"
                        Orientation="{TemplateBinding Orientation}"
                        ViewportSize="{TemplateBinding ViewportSize}"
                        Value="{TemplateBinding Value,
                                                Mode=TwoWay}">
                        <Thumb Name="thumb" />
                    </Track>
                </ControlTemplate>
            </Setter>
        </Style>

        <Style Selector="^ /template/ Thumb#thumb">
            <Setter Property="Background" Value="{DynamicResource ScrollbarThumbColor}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ScrollbarThumbBorderColor}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding CornerRadius}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style Selector="^ /template/ Thumb#thumb:pointerover">
            <Setter Property="Background" Value="{DynamicResource AccentColor}" />
        </Style>
        <Style Selector="^ /template/ Thumb#thumb:pressed">
            <Setter Property="Background" Value="{DynamicResource SecondaryAccentColor}" />
        </Style>
        <Style Selector="^:horizontal /template/ Thumb#thumb">
            <Setter Property="MinWidth" Value="{StaticResource ScrollBarThickness}" />
            <Setter Property="Height" Value="16" />
        </Style>
        <Style Selector="^:vertical /template/ Thumb#thumb">
            <Setter Property="MinHeight" Value="{DynamicResource ScrollBarThickness}" />
            <Setter Property="Width" Value="12" />
        </Style>

    </ControlTheme>

    <ControlTheme x:Key="FloatingSlim" TargetType="ScrollBar">
        <Setter Property="Cursor" Value="Arrow" />

        <Style Selector="^:horizontal">
            <Setter Property="Template">
                <ControlTemplate>
                    <Panel>
                        <Track
                            DeferThumbDrag="{TemplateBinding ScrollViewer.IsDeferredScrollingEnabled}"
                            Maximum="{TemplateBinding Maximum}"
                            Minimum="{TemplateBinding Minimum}"
                            Orientation="{TemplateBinding Orientation}"
                            ViewportSize="{TemplateBinding ViewportSize}"
                            Value="{TemplateBinding Value,
                                                    Mode=TwoWay}">
                            <Thumb
                                Name="thumb"
                                Margin="0,0,0,2"
                                VerticalAlignment="Bottom" />
                        </Track>
                    </Panel>
                </ControlTemplate>
            </Setter>
        </Style>

        <Style Selector="^:vertical">
            <Setter Property="Template">
                <ControlTemplate>
                    <Track
                        DeferThumbDrag="{TemplateBinding ScrollViewer.IsDeferredScrollingEnabled}"
                        Maximum="{TemplateBinding Maximum}"
                        Minimum="{TemplateBinding Minimum}"
                        Orientation="{TemplateBinding Orientation}"
                        ViewportSize="{TemplateBinding ViewportSize}"
                        Value="{TemplateBinding Value,
                                                Mode=TwoWay}">
                        <Thumb Name="thumb" />
                    </Track>
                </ControlTemplate>
            </Setter>
        </Style>

        <Style Selector="^ /template/ Thumb#thumb">
            <Setter Property="Background" Value="{DynamicResource ScrollbarThumbColor}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ScrollbarThumbBorderColor}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding CornerRadius}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style Selector="^ /template/ Thumb#thumb:pointerover">
            <Setter Property="Background" Value="{DynamicResource AccentColor}" />
        </Style>
        <Style Selector="^ /template/ Thumb#thumb:pressed">
            <Setter Property="Background" Value="{DynamicResource SecondaryAccentColor}" />
        </Style>
        <Style Selector="^:horizontal /template/ Thumb#thumb">
            <Setter Property="MinWidth" Value="{StaticResource ScrollBarThickness}" />
            <Setter Property="Height" Value="6" />
        </Style>
        <Style Selector="^:vertical /template/ Thumb#thumb">
            <Setter Property="MinHeight" Value="{DynamicResource ScrollBarThickness}" />
            <Setter Property="Width" Value="12" />
        </Style>

    </ControlTheme>
</ResourceDictionary>