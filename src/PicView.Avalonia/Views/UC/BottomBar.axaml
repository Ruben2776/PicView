<UserControl
    x:Class="PicView.Avalonia.Views.UC.BottomBar"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:customControls="clr-namespace:PicView.Avalonia.CustomControls"
    xmlns:viewModels="clr-namespace:PicView.Core.ViewModels;assembly=PicView.Core"
    Height="{CompiledBinding BottombarHeight.Value}"
    x:DataType="viewModels:MainWindowViewModel"
    IsVisible="{CompiledBinding IsBottomToolbarShown.Value}">

    <UserControl.Resources>

        <ContextMenu x:Key="MainContextMenu">

            <!--  Next  -->
            <customControls:StyledMenuItem
                Command="{CompiledBinding WindowTabs.NextFile}"
                Header="{CompiledBinding Translation.NextImage.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.CanNavigateForwards.Value,
                                            FallbackValue=False}"
                StaysOpenOnClick="True">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource NextImage}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <!--  Prev  -->
            <customControls:StyledMenuItem
                Command="{CompiledBinding WindowTabs.PrevFile}"
                Header="{CompiledBinding Translation.PrevImage,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.CanNavigateBackwards.Value,
                                            FallbackValue=False}"
                StaysOpenOnClick="True">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource PrevImage}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <Separator />


            <!--  Next 10  -->
            <customControls:StyledMenuItem
                Command="{CompiledBinding WindowTabs.Next10}"
                Header="{CompiledBinding Translation.AdvanceBy10Images,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.CanNavigateForwards.Value}"
                StaysOpenOnClick="True">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource NextImage}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <!--  Next 100  -->
            <customControls:StyledMenuItem
                Command="{CompiledBinding WindowTabs.Next100}"
                Header="{CompiledBinding Translation.AdvanceBy100Images.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.CanNavigateForwards.Value}"
                StaysOpenOnClick="True">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource NextImage}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <!--  Prev 10  -->
            <customControls:StyledMenuItem
                Command="{CompiledBinding WindowTabs.Prev10}"
                Header="{CompiledBinding Translation.GoBackBy10Images.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.CanNavigateBackwards.Value}"
                StaysOpenOnClick="True">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource PrevImage}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <!--  Prev 100  -->
            <customControls:StyledMenuItem
                Command="{CompiledBinding WindowTabs.Prev100}"
                Header="{CompiledBinding Translation.GoBackBy100Images.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.CanNavigateBackwards.Value}"
                StaysOpenOnClick="True">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource PrevImage}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <Separator />

            <!--  First Image  -->
            <customControls:StyledMenuItem
                Command="{CompiledBinding WindowTabs.FirstFile}"
                Header="{CompiledBinding Translation.FirstImage.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.CanNavigateBackwards.Value}">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource FirstImage}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <!--  Last Image  -->
            <customControls:StyledMenuItem
                Command="{CompiledBinding WindowTabs.LastFile}"
                Header="{CompiledBinding Translation.LastImage.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.CanNavigateForwards.Value}">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource LastImage}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <Separator />

            <!--  Next folder  -->
            <customControls:StyledMenuItem
                Command="{CompiledBinding NextFolderCommand}"
                Header="{CompiledBinding Translation.NextFolder.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value,
                                            Converter={x:Static ObjectConverters.IsNotNull},
                                            FallbackValue=False}">
                <MenuItem.Icon>
                    <Image
                        Width="14"
                        Height="14"
                        Source="{StaticResource NextFolderImage}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <!--  Prev folder  -->
            <customControls:StyledMenuItem
                Command="{CompiledBinding PrevFolderCommand}"
                Header="{CompiledBinding Translation.PrevFolder.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value,
                                            Converter={x:Static ObjectConverters.IsNotNull},
                                            FallbackValue=False,
                                            Mode=OneWay}">
                <MenuItem.Icon>
                    <Image
                        Width="14"
                        Height="14"
                        Source="{StaticResource PrevFolderImage}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <Separator />

            <!--  Next archive  -->
            <customControls:StyledMenuItem
                Command="{CompiledBinding NextArchiveCommand}"
                Header="{CompiledBinding Translation.NextArchive.Value,
                                         Mode=OneWay}"
                IsEnabled="{Binding WindowTabs.ActiveTab.Value.FileInfo.Value, Converter={x:Static ObjectConverters.IsNotNull}}">
                <MenuItem.Icon>
                    <Image
                        Width="14"
                        Height="14"
                        Source="{StaticResource ArchiveFile}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <!--  Prev archive  -->
            <customControls:StyledMenuItem
                Command="{CompiledBinding PrevArchiveCommand}"
                Header="{CompiledBinding Translation.PrevArchive.Value,
                                         Mode=OneWay}"
                IsEnabled="{Binding WindowTabs.ActiveTab.Value.FileInfo.Value, Converter={x:Static ObjectConverters.IsNotNull}}">
                <MenuItem.Icon>
                    <Image
                        Width="14"
                        Height="14"
                        Source="{StaticResource ArchiveFile}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <Separator />

            <!--  Slideshow  -->
            <customControls:StyledMenuItem
                Command="{CompiledBinding SlideshowCommand}"
                Header="{CompiledBinding Translation.Slideshow.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value,
                                            Converter={x:Static ObjectConverters.IsNotNull},
                                            FallbackValue=False,
                                            Mode=OneWay}">
                <MenuItem.Icon>
                    <Image
                        Width="14"
                        Height="14"
                        Source="{StaticResource SlideshowImage}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <Separator />

            <!--  Hide bottom bar  -->
            <customControls:StyledMenuItem Command="{Binding ToggleBottomToolbarCommand}" Header="{CompiledBinding Translation.HideBottomToolbar.Value, Mode=OneWay}">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource Panel-Bottom}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

        </ContextMenu>

        <ContextMenu x:Key="RotationContextMenu">
            <customControls:StyledMenuItem
                x:Name="Rotation0Item"
                Command="{Binding Rotate0Command}"
                GroupName="RotationGroup"
                Header="0°"
                IsChecked="{Binding WindowTabs.ActiveTab.Value.IsRotated0.Value}"
                ToggleType="Radio" />
            <customControls:StyledMenuItem
                x:Name="Rotation90Item"
                Command="{Binding Rotate90Command}"
                GroupName="RotationGroup"
                Header="90°"
                IsChecked="{Binding WindowTabs.ActiveTab.Value.IsRotated90.Value}"
                ToggleType="Radio" />
            <customControls:StyledMenuItem
                x:Name="Rotation180Item"
                Command="{Binding Rotate180Command}"
                GroupName="RotationGroup"
                Header="180°"
                IsChecked="{Binding WindowTabs.ActiveTab.Value.IsRotated180.Value}"
                ToggleType="Radio" />
            <customControls:StyledMenuItem
                x:Name="Rotation270Item"
                Command="{Binding Rotate270Command}"
                GroupName="RotationGroup"
                Header="270°"
                IsChecked="{Binding WindowTabs.ActiveTab.Value.IsRotated270.Value}"
                ToggleType="Radio" />
            <Separator />
            <customControls:StyledMenuItem Command="{CompiledBinding FlipCommand}" Header="{CompiledBinding Translation.IsFlipped.Value}">
                <MenuItem.Icon>
                    <Path
                        Width="12"
                        Height="12"
                        Data="{StaticResource FlipGeometry}"
                        Fill="{DynamicResource MainTextColor}"
                        Stretch="Fill">
                        <Path.RenderTransform>
                            <ScaleTransform ScaleX="{CompiledBinding WindowTabs.ActiveTab.Value.ScaleX.Value}" />
                        </Path.RenderTransform>
                    </Path>
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <Separator />

            <!--  Hide bottom bar  -->
            <customControls:StyledMenuItem Command="{Binding ToggleBottomToolbarCommand}" Header="{CompiledBinding Translation.HideBottomToolbar.Value, Mode=OneWay}">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource Panel-Bottom}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>
        </ContextMenu>

        <ContextMenu x:Key="SettingsContextMenu">
            <!--  Change background  -->
            <customControls:StyledMenuItem
                Command="{CompiledBinding ChangeBackgroundCommand}"
                Header="{CompiledBinding Translation.ChangeBackground.Value,
                                         Mode=OneWay}"
                StaysOpenOnClick="True">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource PaletteImage}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <Separator />

            <!--  Toggle Subdirectories  -->
            <customControls:StyledMenuItem
                Command="{CompiledBinding ToggleSubdirectoriesCommand}"
                Header="{CompiledBinding Translation.SearchSubdirectory.Value,
                                         Mode=OneWay}"
                IsChecked="{Binding GlobalSettings.IsIncludingSubdirectories.Value}"
                ToggleType="CheckBox" />

            <Separator />

            <!--  Toggle looping  -->
            <customControls:StyledMenuItem
                Command="{CompiledBinding ToggleLoopingCommand}"
                Header="{CompiledBinding Translation.IsLooping.Value,
                                         Mode=OneWay}"
                IsChecked="{Binding GlobalSettings.IsLooping.Value}"
                ToggleType="CheckBox" />

            <Separator />

            <!--  Toggle scrolling  -->
            <customControls:StyledMenuItem
                Header="{CompiledBinding Translation.IsScrolling.Value,
                                         Mode=OneWay}"
                IsChecked="{CompiledBinding IsScrollingEnabled.Value}"
                ToggleType="CheckBox" />

            <!--  Zoom to fit  -->
            <customControls:StyledMenuItem
                Command="{CompiledBinding ZoomToFitCommand}"
                Header="{CompiledBinding Translation.ZoomToFit.Value,
                                         Mode=OneWay}"
                IsChecked="{CompiledBinding IsZoomedToFit.Value}"
                ToggleType="CheckBox" />

            <Separator />

            <!--  Toggle topmost  -->
            <customControls:StyledMenuItem
                Command="{CompiledBinding SetTopMostCommand}"
                Header="{CompiledBinding Translation.StayTopMost.Value,
                                         Mode=OneWay}"
                IsChecked="{CompiledBinding IsTopMost.Value}"
                ToggleType="CheckBox" />

            <!--  Toggle auto fit  -->
            <customControls:StyledMenuItem
                Command="{CompiledBinding AutoFitWindowCommand}"
                Header="{CompiledBinding Translation.AutoFitWindow.Value,
                                         Mode=OneWay}"
                IsChecked="{CompiledBinding IsAutoFit.Value}"
                ToggleType="CheckBox" />

            <!--  Toggle ctrl zoom  -->
            <customControls:StyledMenuItem Command="{CompiledBinding ChangeCtrlZoomCommand}" Header="{CompiledBinding Translation.IsCtrlToZoom.Value, Mode=OneWay}">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{CompiledBinding ChangeCtrlZoomImage.Value}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <Separator />

            <!--  Application shortcuts  -->
            <customControls:StyledMenuItem Command="{Binding KeybindingsWindowCommand}" Header="{CompiledBinding Translation.ApplicationShortcuts.Value, Mode=OneWay}">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource KeyImage}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <!--  Exif window  -->
            <customControls:StyledMenuItem Command="{CompiledBinding ImageInfoWindowCommand}" Header="{CompiledBinding Translation.ImageInfo.Value, Mode=OneWay}">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource CameraImage}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <!--  Image effects  -->
            <customControls:StyledMenuItem
                Command="{Binding EffectsWindowCommand}"
                Header="{CompiledBinding Translation.Effects.Value,
                                         Mode=OneWay}"
                ToolTip.Tip="{CompiledBinding Translation.EffectsTooltip.Value,
                                              Mode=OneWay}">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource SparklesImage}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <!--  Resize  Image  -->
            <MenuItem Command="{Binding ResizeWindowCommand}" Header="{CompiledBinding Translation.ResizeImage.Value, Mode=OneWay}">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource UpscaleImage}" />
                </MenuItem.Icon>
            </MenuItem>

            <!--  Batch Resize  Images  -->
            <customControls:StyledMenuItem Command="{Binding BatchResizeWindowCommand}" Header="{CompiledBinding Translation.BatchResize.Value, Mode=OneWay}">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource UpscaleImage}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <!--  Show about window  -->
            <customControls:StyledMenuItem Command="{CompiledBinding AboutWindowCommand}" Header="{CompiledBinding Translation.About.Value, Mode=OneWay}">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource AboutImage}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <!--  Show Settings window  -->
            <customControls:StyledMenuItem Command="{CompiledBinding SettingsWindowCommand}" Header="{CompiledBinding Translation.ShowAllSettingsWindow.Value, Mode=OneWay}">
                <MenuItem.Icon>
                    <Image Classes="CogImage" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <Separator />

            <!--  Toggle UI  -->
            <customControls:StyledMenuItem Command="{Binding ToggleInterfaceCommand}" Header="{Binding Translation.IsShowingUI.Value, Mode=OneWay}">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource EyeOffImage}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <!--  Hide bottom bar  -->
            <customControls:StyledMenuItem Command="{Binding ToggleBottomToolbarCommand}" Header="{CompiledBinding Translation.HideBottomToolbar.Value, Mode=OneWay}">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource Panel-Bottom}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>
        </ContextMenu>

        <ContextMenu x:Key="OpenMenu">
            <!--  Open  -->
            <customControls:StyledMenuItem
                MinWidth="180"
                Command="{CompiledBinding OpenCommand}"
                Header="{CompiledBinding Translation.Open.Value,
                                         Mode=OneWay}">
                <MenuItem.Icon>
                    <Image Classes="OpenImage" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <!--  Save  -->
            <customControls:StyledMenuItem
                Command="{CompiledBinding SaveCommand}"
                Header="{CompiledBinding Translation.Save.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.Image.Value,
                                            Converter={x:Static ObjectConverters.IsNotNull},
                                            FallbackValue=False}">
                <MenuItem.Icon>
                    <Path Classes="SavePath" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <!--  Save as  -->
            <customControls:StyledMenuItem
                Command="{CompiledBinding SaveAsCommand}"
                Header="{CompiledBinding Translation.SaveAs.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.Image.Value,
                                            Converter={x:Static ObjectConverters.IsNotNull}}">
                <MenuItem.Icon>
                    <Path Classes="SavePath" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <!--  Print  -->
            <customControls:StyledMenuItem
                x:Name="PrintMenuItem"
                Command="{CompiledBinding PrintCommand}"
                Header="{CompiledBinding Translation.Print.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.Image.Value,
                                            Converter={x:Static ObjectConverters.IsNotNull},
                                            FallbackValue=False,
                                            Mode=OneWay}">
                <MenuItem.Icon>
                    <Path Classes="PrintPath" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <!--  Open with  -->
            <customControls:StyledMenuItem
                x:Name="OpenWithMenuItem"
                Command="{CompiledBinding OpenWithCommand}"
                Header="{CompiledBinding Translation.OpenWith.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value,
                                            Converter={x:Static ObjectConverters.IsNotNull},
                                            FallbackValue=False}">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource OpenExternalImage}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <!--  Locate on disk  -->
            <customControls:StyledMenuItem
                Command="{CompiledBinding OpenInExplorerCommand}"
                Header="{CompiledBinding Translation.ShowInFolder.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value,
                                            Converter={x:Static ObjectConverters.IsNotNull},
                                            FallbackValue=False}">
                <MenuItem.Icon>
                    <Path Classes="ShowInFolderPath" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <Separator />

            <!--  Delete file  -->
            <customControls:StyledMenuItem
                x:Name="DeleteFileMenuItem"
                Command="{CompiledBinding DeleteFileCommand}"
                Header="{CompiledBinding Translation.DeleteFile.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value,
                                            Converter={x:Static ObjectConverters.IsNotNull},
                                            FallbackValue=False,
                                            Mode=OneWay}">
                <MenuItem.Icon>
                    <Path Classes="RecyclePath" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <Separator />

            <!--  Paste  -->
            <customControls:StyledMenuItem Command="{CompiledBinding PasteCommand}" Header="{CompiledBinding Translation.Paste.Value, Mode=OneWay}">
                <MenuItem.Icon>
                    <Path Classes="PastePath" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <!--  Copy  -->
            <customControls:StyledMenuItem Header="{CompiledBinding Translation.Copy.Value, Mode=OneWay}">
                <MenuItem.Icon>
                    <Path Classes="CopyPath" />
                </MenuItem.Icon>

                <!--  Copy image  -->
                <customControls:StyledMenuItem Header="{CompiledBinding Translation.CopyImage.Value, Mode=OneWay}" IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.Image.Value, Converter={x:Static ObjectConverters.IsNotNull}, FallbackValue=False, Mode=OneWay}">
                    <MenuItem.Icon>
                        <Path Classes="CopyPath" />
                    </MenuItem.Icon>
                </customControls:StyledMenuItem>

                <!--  Copy file path  -->
                <customControls:StyledMenuItem
                    Command="{CompiledBinding CopyFileCommand}"
                    Header="{Binding Translation.FileCopyPath.Value, Mode=OneWay}"
                    IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value,
                                                Converter={x:Static ObjectConverters.IsNotNull},
                                                FallbackValue=False,
                                                Mode=OneWay}">
                    <MenuItem.Icon>
                        <Path Classes="CopyPath" />
                    </MenuItem.Icon>
                </customControls:StyledMenuItem>

                <!--  Copy base64  -->
                <customControls:StyledMenuItem Command="{CompiledBinding CopyBase64Command}" IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.Image.Value, Converter={x:Static ObjectConverters.IsNotNull}, FallbackValue=False, Mode=OneWay}">
                    <MenuItem.Header>
                        <TextBlock>
                            <Run Text="{CompiledBinding Translation.Copy.Value, Mode=OneWay}" />
                            <Run Text=" base64" />
                        </TextBlock>
                    </MenuItem.Header>
                    <MenuItem.Icon>
                        <Path Classes="CopyPath" />
                    </MenuItem.Icon>
                </customControls:StyledMenuItem>

                <Separator />

                <!--  Duplicate file  -->
                <customControls:StyledMenuItem
                    Command="{CompiledBinding DuplicateFileCommand}"
                    Header="{CompiledBinding Translation.DuplicateFile.Value,
                                             Mode=OneWay}"
                    IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value,
                                                Converter={x:Static ObjectConverters.IsNotNull},
                                                FallbackValue=False,
                                                Mode=OneWay}">
                    <MenuItem.Icon>
                        <Path Classes="DuplicatePath" />
                    </MenuItem.Icon>
                </customControls:StyledMenuItem>

            </customControls:StyledMenuItem>

            <!--  Copy file  -->
            <customControls:StyledMenuItem
                Command="{CompiledBinding CopyFileCommand}"
                Header="{CompiledBinding Translation.CopyFile.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value,
                                            Converter={x:Static ObjectConverters.IsNotNull},
                                            FallbackValue=False,
                                            Mode=OneWay}">
                <MenuItem.Icon>
                    <Path Classes="CopyPath" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>

            <Separator />

            <!--  Hide bottom bar  -->
            <customControls:StyledMenuItem Command="{Binding ToggleBottomToolbarCommand}" Header="{CompiledBinding Translation.HideBottomToolbar.Value, Mode=OneWay}">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource Panel-Bottom}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>
        </ContextMenu>

        <ContextMenu x:Key="BottomMenu">
            <!--  Hide bottom bar  -->
            <customControls:StyledMenuItem Command="{Binding ToggleBottomToolbarCommand}" Header="{CompiledBinding Translation.HideBottomToolbar.Value, Mode=OneWay}">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource Panel-Bottom}" />
                </MenuItem.Icon>
            </customControls:StyledMenuItem>
        </ContextMenu>

    </UserControl.Resources>

    <Border
        x:Name="MainBottomBorder"
        Height="{CompiledBinding BottombarHeight.Value,
                                 Mode=OneWay}"
        Background="{DynamicResource BottomBarBackgroundColor}"
        BorderBrush="{DynamicResource BottomBarBorderColor}"
        BorderThickness="0,1,0,0"
        ContextMenu="{StaticResource BottomMenu}">
        <Panel>
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">

                <customControls:IconButton
                    x:Name="FileMenuButton"
                    Width="38"
                    Height="{CompiledBinding BottombarHeight.Value}"
                    Background="{DynamicResource BottomBarButtonBackgroundColor}"
                    BorderBrush="{DynamicResource BottomBarButtonBorderColor}"
                    BorderThickness="1,0,1,0"
                    Classes="BottomBarHover"
                    ContextMenu="{StaticResource OpenMenu}"
                    Foreground="{DynamicResource BottomBarForegroundColor}"
                    Icon="{StaticResource OpenFolderImage}"
                    IconHeight="17"
                    IconWidth="20"
                    IsTabStop="False"
                    ToolTip.HorizontalOffset="15"
                    ToolTip.Tip="{CompiledBinding Translation.OpenFileDialog.Value}" />

                <customControls:IconButton
                    x:Name="ZoomOutButton"
                    Width="38"
                    Height="{CompiledBinding BottombarHeight.Value}"
                    Background="{DynamicResource BottomBarToolBtnBackground}"
                    BorderBrush="{DynamicResource BottomBarButtonBorderColor}"
                    BorderThickness="0,0,1,0"
                    Classes="BottomBarToolHover"
                    Command="{CompiledBinding ZoomOutCommand}"
                    ContextMenu="{StaticResource RotationContextMenu}"
                    Foreground="{DynamicResource BottomBarForegroundColor}"
                    Icon="{StaticResource ZoomOutImage}"
                    IconHeight="16"
                    IconWidth="18"
                    IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.Image.Value,
                                                Converter={x:Static ObjectConverters.IsNotNull},
                                                FallbackValue=False,
                                                Mode=OneWay}"
                    IsTabStop="False"
                    ToolTip.HorizontalOffset="15"
                    ToolTip.Tip="{CompiledBinding Translation.ZoomOut.Value}" />

                <customControls:IconButton
                    x:Name="ZoomInButton"
                    Width="38"
                    Height="{CompiledBinding BottombarHeight.Value}"
                    Background="{DynamicResource BottomBarToolBtnBackground}"
                    BorderBrush="{DynamicResource BottomBarButtonBorderColor}"
                    BorderThickness="0,0,1,0"
                    Classes="BottomBarToolHover"
                    Command="{CompiledBinding ZoomInCommand}"
                    ContextMenu="{StaticResource RotationContextMenu}"
                    Foreground="{DynamicResource BottomBarForegroundColor}"
                    Icon="{StaticResource ZoomImage}"
                    IconHeight="16"
                    IconWidth="18"
                    IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.Image.Value,
                                                Converter={x:Static ObjectConverters.IsNotNull},
                                                FallbackValue=False,
                                                Mode=OneWay}"
                    IsTabStop="False"
                    ToolTip.HorizontalOffset="15"
                    ToolTip.Tip="{CompiledBinding Translation.ZoomIn.Value}" />

                <customControls:IconButton
                    x:Name="ResetZoomButton"
                    Width="38"
                    Height="25"
                    Background="{DynamicResource BottomBarToolBtnBackground}"
                    BorderBrush="{DynamicResource BottomBarButtonBorderColor}"
                    BorderThickness="0,0,1,0"
                    Classes="BottomBarToolHover"
                    Command="{CompiledBinding ResetZoomAndRotationsCommand}"
                    ContextMenu="{StaticResource RotationContextMenu}"
                    Foreground="{DynamicResource BottomBarForegroundColor}"
                    Icon="{StaticResource RefreshCcwOff}"
                    IconHeight="17"
                    IconWidth="17"
                    IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.Image.Value,
                                                Converter={x:Static ObjectConverters.IsNotNull},
                                                FallbackValue=False,
                                                Mode=OneWay}"
                    IsTabStop="False"
                    ToolTip.HorizontalOffset="15"
                    ToolTip.Tip="{CompiledBinding Translation.ResetZoom.Value}" />

                <customControls:IconButton
                    x:Name="PreviousButton"
                    Width="80"
                    Height="{CompiledBinding BottombarHeight.Value}"
                    Background="{DynamicResource BottomBarNavBtnBackground}"
                    BorderBrush="{DynamicResource BottomBarButtonBorderColor}"
                    BorderThickness="0,0,1,0"
                    Classes="hover"
                    Command="{CompiledBinding WindowTabs.PrevFile}"
                    ContextMenu="{StaticResource MainContextMenu}"
                    Data="{StaticResource FilledArrowLeftGeometry}"
                    Focusable="False"
                    Foreground="{DynamicResource BottomBarForegroundColor}"
                    IconHeight="20"
                    IconWidth="17"
                    IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.CanNavigateBackwards.Value,
                                                FallbackValue=False}"
                    IsTabStop="False" />

                <customControls:IconButton
                    x:Name="NextButton"
                    Width="80"
                    Height="{CompiledBinding BottombarHeight.Value}"
                    Background="{DynamicResource BottomBarNavBtnBackground}"
                    BorderBrush="{DynamicResource BottomBarButtonBorderColor}"
                    BorderThickness="0,0,1,0"
                    Classes="hover"
                    Command="{CompiledBinding WindowTabs.NextFile}"
                    ContextMenu="{StaticResource MainContextMenu}"
                    Data="{StaticResource FilledArrowRightGeometry}"
                    Focusable="False"
                    Foreground="{DynamicResource BottomBarForegroundColor}"
                    IconHeight="20"
                    IconWidth="17"
                    IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.CanNavigateForwards.Value,
                                                FallbackValue=False}"
                    IsTabStop="False" />

                <customControls:IconButton
                    x:Name="RotateLeftButton"
                    Width="38"
                    Height="{CompiledBinding BottombarHeight.Value}"
                    Margin="0"
                    Background="{DynamicResource BottomBarToolBtnBackground}"
                    BorderBrush="{DynamicResource BottomBarButtonBorderColor}"
                    BorderThickness="0,0,1,0"
                    Classes="BottomBarToolHover"
                    Command="{CompiledBinding RotateLeftCommand}"
                    ContextMenu="{StaticResource RotationContextMenu}"
                    DockPanel.Dock="Left"
                    Foreground="{DynamicResource BottomBarForegroundColor}"
                    Icon="{StaticResource RotateCcwImage}"
                    IconHeight="16"
                    IconWidth="16"
                    IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.Image.Value,
                                                Converter={x:Static ObjectConverters.IsNotNull},
                                                FallbackValue=False,
                                                Mode=OneWay}"
                    IsRepeatEnabled="False"
                    ToolTip.HorizontalOffset="15"
                    ToolTip.Tip="{CompiledBinding Translation.RotateLeft.Value,
                                                  Mode=OneWay}" />

                <customControls:IconButton
                    x:Name="RotateRightButton"
                    Width="38"
                    Height="{CompiledBinding BottombarHeight.Value}"
                    Margin="0"
                    Background="{DynamicResource BottomBarToolBtnBackground}"
                    BorderBrush="{DynamicResource BottomBarButtonBorderColor}"
                    Classes="BottomBarToolHover"
                    Command="{CompiledBinding RotateRightCommand}"
                    ContextMenu="{StaticResource RotationContextMenu}"
                    DockPanel.Dock="Left"
                    Foreground="{DynamicResource BottomBarForegroundColor}"
                    Icon="{StaticResource RotateCwImage}"
                    IconHeight="16"
                    IconWidth="16"
                    IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.Image.Value,
                                                Converter={x:Static ObjectConverters.IsNotNull},
                                                FallbackValue=False,
                                                Mode=OneWay}"
                    IsRepeatEnabled="False"
                    ToolTip.HorizontalOffset="15"
                    ToolTip.Tip="{CompiledBinding Translation.RotateRight.Value,
                                                  Mode=OneWay}" />

                <customControls:IconButton
                    x:Name="FlipButton"
                    Width="38"
                    Height="{CompiledBinding BottombarHeight.Value}"
                    Margin="0"
                    Background="{DynamicResource BottomBarToolBtnBackground}"
                    BorderBrush="{DynamicResource BottomBarButtonBorderColor}"
                    BorderThickness="1,0,1,0"
                    Classes="BottomBarToolHover"
                    Command="{CompiledBinding FlipCommand}"
                    ContextMenu="{StaticResource RotationContextMenu}"
                    DockPanel.Dock="Left"
                    Foreground="{DynamicResource BottomBarForegroundColor}"
                    Icon="{StaticResource RepeatImage}"
                    IconHeight="16"
                    IconWidth="16"
                    IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.Image.Value,
                                                Converter={x:Static ObjectConverters.IsNotNull},
                                                FallbackValue=False,
                                                Mode=OneWay}"
                    IsRepeatEnabled="False"
                    ToolTip.HorizontalOffset="15"
                    ToolTip.Tip="{CompiledBinding Translation.IsFlipped.Value,
                                                  Mode=OneWay}">
                    <Button.RenderTransform>
                        <ScaleTransform ScaleX="{Binding WindowTabs.ActiveTab.Value.ScaleX.Value}" />
                    </Button.RenderTransform>
                </customControls:IconButton>

                <customControls:IconButton
                    x:Name="SettingsMenuButton"
                    Width="38"
                    Height="{CompiledBinding BottombarHeight.Value}"
                    Background="{DynamicResource BottomBarButtonBackgroundColor}"
                    BorderBrush="{DynamicResource BottomBarButtonBorderColor}"
                    BorderThickness="0,0,1,0"
                    Classes="BottomBarHover"
                    ContextMenu="{StaticResource SettingsContextMenu}"
                    Foreground="{DynamicResource BottomBarForegroundColor}"
                    Icon="{StaticResource CogImage}"
                    IconHeight="18"
                    IconWidth="18"
                    IsTabStop="False"
                    ToolTip.HorizontalOffset="15"
                    ToolTip.Tip="{CompiledBinding Translation.Settings.Value}"
                    ZIndex="1" />

            </StackPanel>

            <Path
                Width="12"
                Height="12"
                Margin="0,0,2,2"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Data="{StaticResource GripGeometry}"
                Fill="{DynamicResource BottomBarForegroundColor}"
                IsVisible="{Binding !IsAutoFit.Value}"
                Stretch="Fill"
                Stroke="{DynamicResource BottomBarBorderColor}"
                StrokeThickness="1"
                WindowDecorationProperties.ElementRole="ResizeSE" />
        </Panel>
    </Border>
</UserControl>