using Avalonia.Controls;

namespace PicView.Avalonia.Views;

public partial class EffectsView : UserControl
{
    public EffectsView()
    {
        InitializeComponent();
        Loaded += (_, _) => { };
    }
}