<UserControl
    x:Class="PicView.Avalonia.Views.Main.ImageInfoView"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:coreVm="clr-namespace:PicView.Core.ViewModels;assembly=PicView.Core"
    xmlns:customControls="clr-namespace:PicView.Avalonia.CustomControls"
    xmlns:uc="clr-namespace:PicView.Avalonia.Views.UC"
    x:DataType="coreVm:MainWindowViewModel"
    Background="Transparent"
    Foreground="{StaticResource SecondaryTextColor}">
    <UserControl.Styles>
        <Styles>
            <Style Selector="TextBox.x, ComboBox.x">
                <Setter Property="Foreground" Value="{DynamicResource MainTextColor}" />
                <Setter Property="MaxHeight" Value="80" />
            </Style>
            <Style Selector="TextBox.x">
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>
        </Styles>
    </UserControl.Styles>
    <UserControl.ContextMenu>
        <ContextMenu x:Name="MainContextMenu">

            <!--  Save  -->
            <MenuItem
                Command="{CompiledBinding SaveCommand}"
                Header="{CompiledBinding Translation.Save.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.Image.Value,
                                            Converter={x:Static ObjectConverters.IsNotNull},
                                            FallbackValue=False}">
                <MenuItem.Icon>
                    <Path Classes="SavePath" />
                </MenuItem.Icon>
            </MenuItem>

            <!--  Save as  -->
            <MenuItem
                Command="{CompiledBinding SaveAsCommand}"
                Header="{CompiledBinding Translation.SaveAs.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.Image.Value,
                                            Converter={x:Static ObjectConverters.IsNotNull},
                                            FallbackValue=False}">
                <MenuItem.Icon>
                    <Path Classes="SavePath" />
                </MenuItem.Icon>
            </MenuItem>


            <!--  Print  -->
            <MenuItem
                x:Name="PrintMenuItem"
                Command="{CompiledBinding PrintCommand}"
                Header="{CompiledBinding Translation.Print.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.Image.Value,
                                            Converter={x:Static ObjectConverters.IsNotNull},
                                            FallbackValue=False}">
                <MenuItem.Icon>
                    <Path Classes="PrintPath" />
                </MenuItem.Icon>
            </MenuItem>

            <!--  Open with  -->
            <MenuItem
                x:Name="OpenWithMenuItem"
                Command="{CompiledBinding OpenWithCommand}"
                Header="{CompiledBinding Translation.OpenWith.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value,
                                            Converter={x:Static ObjectConverters.IsNotNull},
                                            FallbackValue=False}">
                <MenuItem.Icon>
                    <Image Classes="OpenWithImage" />
                </MenuItem.Icon>
            </MenuItem>

            <!--  Locate on disk  -->
            <MenuItem
                Command="{CompiledBinding OpenInExplorerCommand}"
                Header="{CompiledBinding Translation.ShowInFolder.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value,
                                            Converter={x:Static ObjectConverters.IsNotNull},
                                            FallbackValue=False}">
                <MenuItem.Icon>
                    <Path Classes="ShowInFolderPath" />
                </MenuItem.Icon>
            </MenuItem>

            <!--  Remove image data  -->
            <MenuItem
                x:Name="RemoveImageDataMenuItem"
                Command="{Binding Exif.RemoveImageMetaDataCommand}"
                CommandParameter="{Binding WindowTabs.ActiveTab.Value.FileInfo.Value}"
                Header="{CompiledBinding Translation.RemoveImageData.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value,
                                            Converter={x:Static ObjectConverters.IsNotNull},
                                            FallbackValue=False}">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource OffImage}" />
                </MenuItem.Icon>
            </MenuItem>

            <!--  Set as wallpaper  -->
            <MenuItem
                x:Name="WallpaperMenuItem"
                Header="{CompiledBinding Translation.SetAsWallpaper.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.Image.Value,
                                            Converter={x:Static ObjectConverters.IsNotNull},
                                            FallbackValue=False}"
                IsVisible="{CompiledBinding GlobalSettings.ShowSetAsWallpaper.Value}">
                <MenuItem.Icon>
                    <Path
                        Width="13"
                        Height="10.40"
                        Data="{StaticResource PanoramaGeometry}"
                        Fill="{DynamicResource MainTextColor}"
                        Stretch="Fill" />
                </MenuItem.Icon>

                <!--  Set as wallpaper filled  -->
                <MenuItem
                    Command="{CompiledBinding SetAsWallpaperFilledCommand}"
                    Header="{CompiledBinding Translation.Fill.Value,
                                             Mode=OneWay}"
                    IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.Image.Value,
                                                Converter={x:Static ObjectConverters.IsNotNull},
                                                FallbackValue=False}"
                    IsVisible="{CompiledBinding GlobalSettings.ShowSetAsWallpaper.Value}">
                    <MenuItem.Icon>
                        <Path
                            Width="13"
                            Height="10.40"
                            Data="{StaticResource PanoramaGeometry}"
                            Fill="{DynamicResource MainTextColor}"
                            Stretch="Fill" />
                    </MenuItem.Icon>
                </MenuItem>

                <!--  Set as wallpaper fit  -->
                <MenuItem
                    Command="{CompiledBinding SetAsWallpaperFittedCommand}"
                    Header="{CompiledBinding Translation.Fit.Value,
                                             Mode=OneWay}"
                    IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.Image.Value,
                                                Converter={x:Static ObjectConverters.IsNotNull},
                                                FallbackValue=False}"
                    IsVisible="{CompiledBinding GlobalSettings.ShowSetAsWallpaper.Value}">
                    <MenuItem.Icon>
                        <Path
                            Width="13"
                            Height="10.40"
                            Data="{StaticResource PanoramaGeometry}"
                            Fill="{DynamicResource MainTextColor}"
                            Stretch="Fill" />
                    </MenuItem.Icon>
                </MenuItem>

                <!--  Set as wallpaper stretched  -->
                <MenuItem
                    Command="{CompiledBinding SetAsWallpaperStretchedCommand}"
                    Header="{CompiledBinding Translation.Stretch.Value,
                                             Mode=OneWay}"
                    IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.Image.Value,
                                                Converter={x:Static ObjectConverters.IsNotNull},
                                                FallbackValue=False}"
                    IsVisible="{CompiledBinding GlobalSettings.ShowSetAsWallpaper.Value}">
                    <MenuItem.Icon>
                        <Path
                            Width="13"
                            Height="10.40"
                            Data="{StaticResource PanoramaGeometry}"
                            Fill="{DynamicResource MainTextColor}"
                            Stretch="Fill" />
                    </MenuItem.Icon>
                </MenuItem>


                <!--  Set as wallpaper centered  -->
                <MenuItem
                    Command="{CompiledBinding SetAsWallpaperCenteredCommand}"
                    Header="{CompiledBinding Translation.Center.Value,
                                             Mode=OneWay}"
                    IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.Image.Value,
                                                Converter={x:Static ObjectConverters.IsNotNull},
                                                FallbackValue=False}"
                    IsVisible="{CompiledBinding GlobalSettings.ShowSetAsWallpaper.Value}">
                    <MenuItem.Icon>
                        <Path
                            Width="13"
                            Height="10.40"
                            Data="{StaticResource PanoramaGeometry}"
                            Fill="{DynamicResource MainTextColor}"
                            Stretch="Fill" />
                    </MenuItem.Icon>
                </MenuItem>

                <!--  Set as wallpaper tiled  -->
                <MenuItem
                    Command="{CompiledBinding SetAsWallpaperTiledCommand}"
                    Header="{CompiledBinding Translation.Tile.Value,
                                             Mode=OneWay}"
                    IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.Image.Value,
                                                Converter={x:Static ObjectConverters.IsNotNull},
                                                FallbackValue=False}"
                    IsVisible="{CompiledBinding GlobalSettings.ShowSetAsWallpaper.Value}">
                    <MenuItem.Icon>
                        <Path
                            Width="13"
                            Height="10.40"
                            Data="{StaticResource PanoramaGeometry}"
                            Fill="{DynamicResource MainTextColor}"
                            Stretch="Fill" />
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>

            <!--  File properties  -->
            <MenuItem
                Command="{CompiledBinding ShowFilePropertiesCommand}"
                Header="{CompiledBinding Translation.FileProperties.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value,
                                            Converter={x:Static ObjectConverters.IsNotNull},
                                            FallbackValue=False}">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource FileUserImage}"
                        Stretch="Fill" />
                </MenuItem.Icon>
            </MenuItem>

            <!--  Optimize image  -->
            <MenuItem
                Command="{CompiledBinding OptimizeImageCommand}"
                Header="{CompiledBinding Translation.OptimizeImage.Value,
                                         Mode=OneWay}"
                IsEnabled="True">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource PortalImage}" />
                </MenuItem.Icon>
            </MenuItem>

            <!--  Convert image  -->
            <MenuItem
                Command="{CompiledBinding ConvertWindowCommand}"
                Header="{CompiledBinding Translation.ConvertTo.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value,
                                            Converter={x:Static ObjectConverters.IsNotNull},
                                            FallbackValue=False}">
                <MenuItem.Icon>
                    <Image
                        Width="12"
                        Height="12"
                        Source="{StaticResource FileImage}"
                        Stretch="Fill" />
                </MenuItem.Icon>
            </MenuItem>

            <Separator />

            <!--  Delete file  -->
            <MenuItem
                Command="{CompiledBinding DeleteFileCommand}"
                Header="{CompiledBinding Translation.DeleteFile.Value,
                                         Mode=OneWay}"
                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value,
                                            Converter={x:Static ObjectConverters.IsNotNull},
                                            FallbackValue=False}">
                <MenuItem.Icon>
                    <Path Classes="RecyclePath" />
                </MenuItem.Icon>
            </MenuItem>

            <Separator />

            <!--  Copy  -->
            <MenuItem Command="{CompiledBinding CopyFileCommand}" Header="{CompiledBinding Translation.Copy.Value, Mode=OneWay}">
                <MenuItem.Icon>
                    <Path Classes="CopyPath Xl" />
                </MenuItem.Icon>

                <!--  Copy file  -->
                <MenuItem
                    Command="{CompiledBinding CopyFileCommand}"
                    Header="{CompiledBinding Translation.CopyFile.Value,
                                             Mode=OneWay}"
                    IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value,
                                                Converter={x:Static ObjectConverters.IsNotNull},
                                                FallbackValue=False}">
                    <MenuItem.Icon>
                        <Path Classes="CopyPath Xl" />
                    </MenuItem.Icon>
                </MenuItem>

                <!--  Copy image  -->
                <MenuItem
                    Command="{CompiledBinding CopyImageCommand}"
                    Header="{CompiledBinding Translation.CopyImage.Value,
                                             Mode=OneWay}"
                    IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.Image.Value,
                                                Converter={x:Static ObjectConverters.IsNotNull},
                                                FallbackValue=False}">
                    <MenuItem.Icon>
                        <Path Classes="CopyPath Xl" />
                    </MenuItem.Icon>
                </MenuItem>

                <!--  Copy file path  -->
                <MenuItem
                    Command="{CompiledBinding CopyFilePathCommand}"
                    Header="{Binding Translation.FileCopyPath, Mode=OneWay}"
                    IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value,
                                                Converter={x:Static ObjectConverters.IsNotNull},
                                                FallbackValue=False}">
                    <MenuItem.Icon>
                        <Path Classes="CopyPath Xl" />
                    </MenuItem.Icon>
                </MenuItem>

                <!--  Copy base64  -->
                <MenuItem Command="{CompiledBinding CopyBase64Command}" IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.Image.Value, Converter={x:Static ObjectConverters.IsNotNull}, FallbackValue=False}">
                    <MenuItem.Header>
                        <TextBlock>
                            <Run Text="{CompiledBinding Translation.Copy, Mode=OneWay}" />
                            <Run Text=" base64" />
                        </TextBlock>
                    </MenuItem.Header>
                    <MenuItem.Icon>
                        <Path Classes="CopyPath Xl" />
                    </MenuItem.Icon>
                </MenuItem>

                <Separator />

                <!--  Duplicate file  -->
                <MenuItem
                    Command="{CompiledBinding DuplicateFileCommand}"
                    Header="{CompiledBinding Translation.DuplicateFile,
                                             Mode=OneWay}"
                    IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value,
                                                Converter={x:Static ObjectConverters.IsNotNull},
                                                FallbackValue=False}">
                    <MenuItem.Icon>
                        <Path Classes="DuplicatePath" />
                    </MenuItem.Icon>
                </MenuItem>

            </MenuItem>

            <Separator />

            <!--  Close  -->
            <MenuItem x:Name="CloseItem" Header="{CompiledBinding Translation.Close.Value, Mode=OneWay}">
                <MenuItem.Icon>
                    <Path Classes="ClosePath" />
                </MenuItem.Icon>
            </MenuItem>

        </ContextMenu>
    </UserControl.ContextMenu>

    <Panel>
        <uc:SpinWaiter
            x:Name="SpinWaiter"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            IsVisible="False" />
        <customControls:AutoScrollViewer
            x:Name="ScrollViewer"
            Background="Transparent"
            Focusable="True"
            VerticalScrollBarVisibility="Visible">

            <Panel x:Name="ParentPanel" Margin="5">

                <StackPanel x:Name="MainContainer">

                    <WrapPanel ItemsAlignment="Center">

                        <!--  File name  -->
                        <StackPanel Margin="15,10" Orientation="Horizontal">
                            <TextBlock
                                Width="{CompiledBinding InfoWindow.TextWidth,
                                                        FallbackValue=0}"
                                Margin="0,8,0,0"
                                Classes="txt"
                                Text="{CompiledBinding Translation.FileName.Value,
                                                       Mode=OneWay}" />
                            <customControls:FuncTextBox
                                x:Name="FileNameTextBox"
                                Width="{CompiledBinding InfoWindow.TextBoxXlWidth.Value,
                                                        FallbackValue=0}"
                                Classes="hover TStyle x"
                                Text="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value.Name}" />
                            <customControls:CopyButton
                                Classes="altHover BorderStyle"
                                CopyText="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value}"
                                IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value,
                                                            FallbackValue=True}"
                                ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                              Mode=OneWay}">
                                <Path Classes="CopyPath Xl" />
                            </customControls:CopyButton>
                        </StackPanel>

                        <!--  Folder path  -->
                        <StackPanel Margin="15,10" Orientation="Horizontal">
                            <TextBlock
                                Width="{CompiledBinding InfoWindow.TextWidth,
                                                        FallbackValue=0}"
                                Margin="0,8,0,0"
                                Classes="txt"
                                Text="{CompiledBinding Translation.Folder.Value,
                                                       Mode=OneWay}" />
                            <customControls:FuncTextBox
                                x:Name="DirectoryNameTextBox"
                                Width="{CompiledBinding InfoWindow.TextBoxXlWidth.Value,
                                                        FallbackValue=0}"
                                Classes="hover TStyle x"
                                Text="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value.DirectoryName,
                                                       FallbackValue=''}" />
                            <customControls:CopyButton
                                Classes="altHover BorderStyle"
                                CopyText="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value.DirectoryName,
                                                           FallbackValue=''}"
                                IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value,
                                                            FallbackValue=False}"
                                ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                              Mode=OneWay}">
                                <Path Classes="CopyPath Xl" />
                            </customControls:CopyButton>
                        </StackPanel>

                        <!--  Full path  -->
                        <StackPanel Margin="15,10" Orientation="Horizontal">
                            <TextBlock
                                Width="{CompiledBinding InfoWindow.TextWidth,
                                                        FallbackValue='0'}"
                                Margin="0,8,0,0"
                                Classes="txt"
                                Text="{CompiledBinding Translation.FullPath.Value,
                                                       Mode=OneWay}" />
                            <customControls:FuncTextBox
                                x:Name="FullPathTextBox"
                                Width="{CompiledBinding InfoWindow.TextBoxXlWidth.Value}"
                                Classes="hover TStyle x"
                                Text="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value.FullName,
                                                       FallbackValue=''}" />
                            <customControls:CopyButton
                                Classes="altHover BorderStyle"
                                CopyText="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value.FullName,
                                                           FallbackValue=''}"
                                IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                              Mode=OneWay}">
                                <Path Classes="CopyPath Xl" />
                            </customControls:CopyButton>
                        </StackPanel>

                        <!--  Width  -->
                        <StackPanel Margin="15,10" Orientation="Horizontal">
                            <TextBlock
                                Width="{CompiledBinding InfoWindow.TextWidth}"
                                Margin="0,8,0,0"
                                Classes="txt"
                                Text="{CompiledBinding Translation.Width.Value,
                                                       Mode=OneWay}" />
                            <customControls:NumTextBox
                                x:Name="PixelWidthTextBox"
                                Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                Classes="hover TStyle x"
                                Text="{CompiledBinding Exif.PixelWidth.Value,
                                                       Mode=OneWay}"
                                ToolTip.Tip="{CompiledBinding Translation.SizeTooltip.Value,
                                                              Mode=OneWay}" />
                            <customControls:CopyButton
                                Classes="altHover BorderStyle"
                                CopyText="{CompiledBinding Exif.PixelWidth.Value,
                                                           Mode=OneWay}"
                                IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                ToolTip.Tip="{CompiledBinding Translation.Copy,
                                                              Mode=OneWay}">
                                <Path Classes="CopyPath Xl" />
                            </customControls:CopyButton>
                        </StackPanel>

                        <!--  Height  -->
                        <StackPanel Margin="15,10" Orientation="Horizontal">
                            <TextBlock
                                Width="{CompiledBinding InfoWindow.TextWidth}"
                                Margin="0,8,0,0"
                                Classes="txt"
                                Text="{CompiledBinding Translation.Height.Value,
                                                       Mode=OneWay}" />
                            <customControls:NumTextBox
                                x:Name="PixelHeightTextBox"
                                Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                Classes="hover TStyle x"
                                Text="{CompiledBinding Exif.PixelHeight.Value,
                                                       Mode=OneWay}"
                                ToolTip.Tip="{CompiledBinding Translation.SizeTooltip.Value,
                                                              Mode=OneWay}" />
                            <customControls:CopyButton
                                Classes="altHover BorderStyle"
                                CopyText="{CompiledBinding Exif.PixelHeight.Value,
                                                           Mode=OneWay}"
                                IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                              Mode=OneWay}">
                                <Path Classes="CopyPath Xl" />
                            </customControls:CopyButton>
                        </StackPanel>

                        <!--  Size Mp  -->
                        <StackPanel Margin="15,10" Orientation="Horizontal">
                            <TextBlock
                                Width="{CompiledBinding InfoWindow.TextWidth,
                                                        FallbackValue=''}"
                                Margin="0,8,0,0"
                                Classes="txt"
                                Text="{CompiledBinding Translation.SizeMp.Value,
                                                       Mode=OneWay}" />
                            <customControls:FuncTextBox
                                x:Name="SizeMpTextBox"
                                Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                Background="{DynamicResource DisabledBackgroundColor}"
                                Classes=" TStyle x"
                                IsReadOnly="True"
                                Text="{CompiledBinding Exif.SizeMp.Value,
                                                       Mode=OneWay,
                                                       FallbackValue=''}" />
                            <customControls:CopyButton
                                Classes="altHover BorderStyle"
                                CopyText="{CompiledBinding Exif.SizeMp.Value,
                                                           Mode=OneWay}"
                                IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                              Mode=OneWay}">
                                <Path Classes="CopyPath Xl" />
                            </customControls:CopyButton>
                        </StackPanel>

                        <!--  Resolution  -->
                        <StackPanel Margin="15,10" Orientation="Horizontal">
                            <TextBlock
                                x:Name="ResolutionTextBlock"
                                Width="{CompiledBinding InfoWindow.TextWidth}"
                                Margin="0,8,0,0"
                                Classes="txt"
                                Text="{CompiledBinding Translation.Resolution.Value,
                                                       Mode=OneWay}" />
                            <customControls:FuncTextBox
                                Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                Background="{DynamicResource DisabledBackgroundColor}"
                                Classes=" TStyle x"
                                IsReadOnly="True"
                                Text="{CompiledBinding Exif.Resolution.Value,
                                                       Mode=OneWay}" />
                            <customControls:CopyButton
                                Classes="altHover BorderStyle"
                                CopyText="{CompiledBinding Exif.Resolution.Value,
                                                           Mode=OneWay}"
                                IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                ToolTip.Tip="{CompiledBinding Translation.Copy,
                                                              Mode=OneWay}">
                                <Path Classes="CopyPath Xl" />
                            </customControls:CopyButton>
                        </StackPanel>

                        <!--  Print Size Centimeters  -->
                        <StackPanel Margin="15,10" Orientation="Horizontal">
                            <TextBlock
                                Width="{CompiledBinding InfoWindow.TextWidth}"
                                Margin="0,8,0,0"
                                Classes="txt"
                                Text="{CompiledBinding Translation.PrintSizeCm.Value,
                                                       Mode=OneWay}" />
                            <customControls:FuncTextBox
                                x:Name="PrintSizeCmTextBox"
                                Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                Background="{DynamicResource DisabledBackgroundColor}"
                                Classes=" TStyle x"
                                IsReadOnly="True"
                                Text="{CompiledBinding Exif.PrintSizeCm.Value,
                                                       Mode=OneWay}" />
                            <customControls:CopyButton
                                Classes="altHover BorderStyle"
                                CopyText="{CompiledBinding Exif.PrintSizeCm.Value,
                                                           Mode=OneWay}"
                                IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                              Mode=OneWay}">
                                <Path Classes="CopyPath Xl" />
                            </customControls:CopyButton>
                        </StackPanel>

                        <!--  Print Size Inches  -->
                        <StackPanel Margin="15,10" Orientation="Horizontal">
                            <TextBlock
                                Width="{CompiledBinding InfoWindow.TextWidth}"
                                Margin="0,8,0,0"
                                Classes="txt"
                                Text="{CompiledBinding Translation.PrintSizeIn.Value,
                                                       Mode=OneWay}" />
                            <customControls:FuncTextBox
                                x:Name="PrintSizeInchTextBox"
                                Width="{CompiledBinding InfoWindow.TextBoxWidth.Value,
                                                        FallbackValue=0}"
                                Background="{DynamicResource DisabledBackgroundColor}"
                                Classes=" TStyle x"
                                IsReadOnly="True"
                                Text="{CompiledBinding Exif.PrintSizeInch.Value,
                                                       Mode=OneWay,
                                                       FallbackValue=''}" />
                            <customControls:CopyButton
                                Classes="altHover BorderStyle"
                                CopyText="{CompiledBinding Exif.PrintSizeInch.Value,
                                                           Mode=OneWay,
                                                           FallbackValue=''}"
                                IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value,
                                                            FallbackValue=False}"
                                ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                              Mode=OneWay}">
                                <Path Classes="CopyPath Xl" />
                            </customControls:CopyButton>
                        </StackPanel>

                        <!--  Aspect Ratio  -->
                        <StackPanel Margin="15,10" Orientation="Horizontal">
                            <TextBlock
                                Width="{CompiledBinding InfoWindow.TextWidth,
                                                        FallbackValue=0}"
                                Margin="0,8,0,0"
                                Classes="txt"
                                Text="{CompiledBinding Translation.ReadAbleAspectRatio.Value,
                                                       Mode=OneWay}" />
                            <customControls:FuncTextBox
                                x:Name="AspectRatioTextBox"
                                Width="{CompiledBinding InfoWindow.TextBoxWidth.Value,
                                                        FallbackValue=0}"
                                Background="{DynamicResource DisabledBackgroundColor}"
                                Classes=" TStyle x"
                                IsReadOnly="True"
                                Text="{CompiledBinding Exif.AspectRatio.Value,
                                                       Mode=OneWay,
                                                       FallbackValue=''}" />
                            <customControls:CopyButton
                                Classes="altHover BorderStyle"
                                CopyText="{CompiledBinding Exif.AspectRatio.Value,
                                                           Mode=OneWay,
                                                           FallbackValue=''}"
                                IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value,
                                                            FallbackValue=False}"
                                ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                              Mode=OneWay}">
                                <Path Classes="CopyPath Xl" />
                            </customControls:CopyButton>
                        </StackPanel>

                        <!--  Bit Depth  -->
                        <StackPanel Margin="15,10" Orientation="Horizontal">
                            <TextBlock
                                Width="{CompiledBinding InfoWindow.TextWidth,
                                                        FallbackValue=0}"
                                Margin="0,8,0,0"
                                Classes="txt"
                                Text="{CompiledBinding Translation.BitDepth.Value,
                                                       Mode=OneWay}" />
                            <customControls:FuncTextBox
                                Width="{CompiledBinding InfoWindow.TextBoxWidth.Value,
                                                        FallbackValue=0}"
                                Background="{DynamicResource DisabledBackgroundColor}"
                                Classes="TStyle x"
                                IsReadOnly="True"
                                Text="{CompiledBinding Exif.BitDepth.Value,
                                                       Mode=OneWay,
                                                       FallbackValue=''}" />
                            <customControls:CopyButton
                                Classes="altHover BorderStyle"
                                CopyText="{CompiledBinding Exif.BitDepth.Value,
                                                           Mode=OneWay,
                                                           FallbackValue=''}"
                                IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value,
                                                            FallbackValue=False}"
                                ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                              Mode=OneWay}">
                                <Path Classes="CopyPath Xl" />
                            </customControls:CopyButton>
                        </StackPanel>

                        <!--  Orientation  -->
                        <StackPanel Margin="15,10" Orientation="Horizontal">
                            <TextBlock
                                Width="{CompiledBinding InfoWindow.TextWidth,
                                                        FallbackValue=0}"
                                Margin="0,8,0,0"
                                Classes="txt"
                                Text="{CompiledBinding Translation.Orientation.Value,
                                                       Mode=OneWay}" />
                            <ComboBox
                                x:Name="OrientationBox"
                                Width="{CompiledBinding InfoWindow.TextBoxWidth.Value,
                                                        FallbackValue=0}"
                                MinWidth="180"
                                Margin="5,0,0,0"
                                Background="{DynamicResource DisabledBackgroundColor}"
                                Classes="CStyle x"
                                ItemsSource="{CompiledBinding Exif.Orientations.Value,
                                                              Mode=OneWay,
                                                              FallbackValue=''}"
                                SelectedIndex="{CompiledBinding Exif.Orientation.Value,
                                                                Mode=OneWay,
                                                                FallbackValue=0}" />
                            <customControls:CopyButton
                                Classes="altHover BorderStyle"
                                CopyText="{CompiledBinding Exif.Orientation.Value,
                                                           Mode=OneWay,
                                                           FallbackValue=''}"
                                IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value,
                                                            FallbackValue=False}"
                                ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                              Mode=OneWay}">
                                <Path Classes="CopyPath Xl" />
                            </customControls:CopyButton>
                        </StackPanel>

                        <!--  Image Format  -->
                        <StackPanel Margin="15,10" Orientation="Horizontal">
                            <TextBlock
                                Width="{CompiledBinding InfoWindow.TextWidth,
                                                        FallbackValue=''}"
                                Margin="0,8,0,0"
                                Classes="txt"
                                Text="{CompiledBinding Translation.ImageFormat.Value}" />
                            <customControls:FuncTextBox
                                Width="{CompiledBinding InfoWindow.TextBoxWidth.Value,
                                                        FallbackValue=''}"
                                Background="{DynamicResource DisabledBackgroundColor}"
                                Classes="TStyle x"
                                IsReadOnly="True"
                                Text="{CompiledBinding Exif.ImageFormatDisplay.Value,
                                                       Mode=OneWay}" />
                            <customControls:CopyButton
                                Classes="altHover BorderStyle"
                                CopyText="{CompiledBinding Exif.ImageFormatDisplay.Value,
                                                           Mode=OneWay}"
                                IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value,
                                                            FallbackValue=''}"
                                ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                              Mode=OneWay}">
                                <Path Classes="CopyPath Xl" />
                            </customControls:CopyButton>
                        </StackPanel>

                        <!--  File Size  -->
                        <StackPanel Margin="15,10" Orientation="Horizontal">
                            <TextBlock
                                Width="{CompiledBinding InfoWindow.TextWidth}"
                                Margin="0,8,0,0"
                                Classes="txt"
                                Text="{CompiledBinding Translation.FileSize.Value,
                                                       Mode=OneWay}" />
                            <customControls:FuncTextBox
                                x:Name="FileSizeBox"
                                Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                Background="{DynamicResource DisabledBackgroundColor}"
                                Classes="TStyle x"
                                IsReadOnly="True" />
                            <customControls:CopyButton
                                Classes="altHover BorderStyle"
                                CopyText="{Binding Path=Text, ElementName=FileSizeBox}"
                                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value,
                                                            Converter={x:Static ObjectConverters.IsNotNull}}"
                                IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                              Mode=OneWay}">
                                <Path Classes="CopyPath Xl" />
                            </customControls:CopyButton>
                        </StackPanel>

                        <!--  Created  -->
                        <StackPanel Margin="15,10" Orientation="Horizontal">
                            <TextBlock
                                Width="{CompiledBinding InfoWindow.TextWidth,
                                                        FallbackValue=0}"
                                Margin="0,8,0,0"
                                Classes="txt"
                                Text="{CompiledBinding Translation.Created.Value,
                                                       Mode=OneWay}" />
                            <customControls:DateTimePickerButtons
                                x:Name="CreationTimeBox"
                                Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                Margin="5,0,0,0"
                                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value,
                                                            Converter={x:Static ObjectConverters.IsNotNull}}"
                                SelectedDateTime="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value.CreationTime,
                                                                   Mode=TwoWay,
                                                                   FallbackValue=''}" />

                            <customControls:CopyButton
                                Classes="altHover BorderStyle"
                                CopyText="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value.CreationTime,
                                                           FallbackValue=''}"
                                IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                              Mode=OneWay}">
                                <Path Classes="CopyPath Xl" />
                            </customControls:CopyButton>
                        </StackPanel>

                        <!--  Modified  -->
                        <StackPanel Margin="15,10" Orientation="Horizontal">
                            <TextBlock
                                Width="{CompiledBinding InfoWindow.TextWidth}"
                                Margin="0,8,0,0"
                                Classes="txt"
                                Text="{CompiledBinding Translation.Modified.Value,
                                                       Mode=OneWay}" />
                            <customControls:DateTimePickerButtons
                                x:Name="ModifiedBox"
                                Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                Margin="5,0,0,0"
                                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value,
                                                            Converter={x:Static ObjectConverters.IsNotNull}}"
                                SelectedDateTime="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value.LastWriteTime,
                                                                   Mode=TwoWay,
                                                                   FallbackValue=''}" />
                            <customControls:CopyButton
                                Classes="altHover BorderStyle"
                                CopyText="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value.LastWriteTime,
                                                           FallbackValue=''}"
                                IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                              Mode=OneWay}">
                                <Path Classes="CopyPath Xl" />
                            </customControls:CopyButton>
                        </StackPanel>

                        <!--  Last Accessed  -->
                        <StackPanel Margin="15,10" Orientation="Horizontal">
                            <TextBlock
                                Width="{CompiledBinding InfoWindow.TextWidth}"
                                Margin="0,8,0,0"
                                Classes="txt"
                                Text="{CompiledBinding Translation.LastAccessTime.Value,
                                                       Mode=OneWay}" />
                            <customControls:DateTimePickerButtons
                                x:Name="LastAccessedBox"
                                Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                Margin="5,0,0,0"
                                IsEnabled="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value,
                                                            Converter={x:Static ObjectConverters.IsNotNull}}"
                                SelectedDateTime="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value.LastAccessTime,
                                                                   Mode=TwoWay,
                                                                   FallbackValue=''}" />

                            <customControls:CopyButton
                                Classes="altHover BorderStyle"
                                CopyText="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value.LastAccessTime,
                                                           FallbackValue=''}"
                                IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                              Mode=OneWay}">
                                <Path Classes="CopyPath Xl" />
                            </customControls:CopyButton>
                        </StackPanel>

                    </WrapPanel>

                    <!--  Copyright, comment, etc.  -->
                    <StackPanel IsEnabled="{CompiledBinding Exif.IsExifAvailable.Value}">

                        <StackPanel
                            Height="20"
                            Margin="15,20"
                            HorizontalAlignment="Center"
                            Orientation="Horizontal">
                            <Rectangle
                                Width="{CompiledBinding InfoWindow.HalfLineWidth.Value,
                                                        FallbackValue=0}"
                                Height="1"
                                Margin="0,0,7,0"
                                Fill="{DynamicResource MainBorderColor}" />
                            <Path
                                Data="{StaticResource AtGeometry}"
                                Fill="{DynamicResource MainTextColor}"
                                Stretch="Uniform" />
                            <Rectangle
                                Width="{CompiledBinding InfoWindow.HalfLineWidth.Value,
                                                        FallbackValue=0}"
                                Height="1"
                                Margin="7,0,0,0"
                                Fill="{DynamicResource MainBorderColor}" />
                        </StackPanel>

                        <WrapPanel Margin="5,10,0,0" HorizontalAlignment="Center">

                            <!--  Date taken  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth,
                                                            FallbackValue=0}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.DateTaken.Value,
                                                           Mode=OneWay}" />
                                <customControls:DateTimePickerButtons
                                    x:Name="DateTakenBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value,
                                                            FallbackValue=0}"
                                    Margin="5,0,0,0"
                                    Command="{CompiledBinding Exif.SetDateTakenCommand}"
                                    CommandParameter="{CompiledBinding WindowTabs.ActiveTab.Value.FileInfo.Value}"
                                    SelectedDateTime="{CompiledBinding Exif.DateTaken.Value,
                                                                       FallbackValue=''}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.DateTaken.Value,
                                                               Mode=OneWay,
                                                               FallbackValue=''}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value,
                                                                FallbackValue=False}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Authors  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.Authors.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="AuthorsBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetAuthorsCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.Authors.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.Authors.Value,
                                                               Mode=TwoWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Copyright  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.Copyright.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="CopyRightBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetCopyrightCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.Copyright.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.Copyright.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Software  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.Software.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="SoftwareBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes="TStyle x"
                                    Command="{CompiledBinding Exif.SetSoftwareCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.Software.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.Software.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Subject  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.Subject.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="SubjectBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxXxlWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetSubjectCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.Subject.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.Subject.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Title  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.Title.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="TitleBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxXxlWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetTitleCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.Title.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.Title.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Comment  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="Comment" />
                                <customControls:FuncTextBox
                                    x:Name="CommentBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxXxlWidth.Value}"
                                    MaxHeight="600"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetCommentCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.Comment.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.Comment.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>
                        </WrapPanel>

                    </StackPanel>

                    <!--  Gps  -->
                    <StackPanel IsEnabled="{CompiledBinding Exif.IsExifAvailable.Value}">
                        <StackPanel
                            Height="20"
                            Margin="15,20"
                            HorizontalAlignment="Center"
                            Orientation="Horizontal">
                            <Rectangle
                                Width="{CompiledBinding InfoWindow.HalfLineWidth.Value}"
                                Height="1"
                                Margin="0,0,7,0"
                                Fill="{DynamicResource MainBorderColor}" />
                            <Path
                                Data="{StaticResource GpsGeometry}"
                                Fill="{DynamicResource MainTextColor}"
                                Stretch="Uniform" />
                            <Rectangle
                                Width="{CompiledBinding InfoWindow.HalfLineWidth.Value}"
                                Height="1"
                                Margin="7,0,0,0"
                                Fill="{DynamicResource MainBorderColor}" />
                        </StackPanel>

                        <WrapPanel Margin="5,10,0,0" HorizontalAlignment="Center">

                            <!--  Latitude  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.Latitude.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="LatitudeBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Background="{DynamicResource DisabledBackgroundColor}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetLatitudeCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    IsReadOnly="True"
                                    Text="{CompiledBinding Exif.Latitude.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.Latitude.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Longitude  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.Longitude.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="LongitudeBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Background="{DynamicResource DisabledBackgroundColor}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetLongitudeCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    IsReadOnly="True"
                                    Text="{CompiledBinding Exif.Longitude.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.Longitude.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Altitude  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.Altitude,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="AltitudeBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Background="{DynamicResource DisabledBackgroundColor}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetAltitudeCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    IsReadOnly="True"
                                    Text="{CompiledBinding Exif.Altitude.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.Altitude.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Map link buttons  -->
                            <StackPanel Margin="0,5" Orientation="Horizontal">
                                <Button
                                    x:Name="GoogleLinkButton"
                                    Margin="117,0,15,0"
                                    Classes="altHover BorderStyle"
                                    Command="{CompiledBinding Exif.OpenGoogleLinkCommand,
                                                              Mode=OneWay}">
                                    <Path
                                        Width="20"
                                        Height="20"
                                        Data="{StaticResource GoogleGeometry}"
                                        Fill="{DynamicResource MainTextColor}"
                                        Stretch="Fill" />
                                </Button>
                                <Button
                                    x:Name="BingLinkButton"
                                    Classes="altHover BorderStyle"
                                    Command="{CompiledBinding Exif.OpenBingLinkCommand,
                                                              Mode=OneWay}">
                                    <Path
                                        Width="20"
                                        Height="20"
                                        Data="{StaticResource BingGeometry}"
                                        Fill="{DynamicResource MainTextColor}"
                                        Stretch="Fill" />
                                </Button>

                            </StackPanel>
                        </WrapPanel>

                    </StackPanel>

                    <!--  Color  -->
                    <StackPanel IsEnabled="{CompiledBinding Exif.IsExifAvailable.Value}">
                        <StackPanel
                            Height="20"
                            Margin="15,20"
                            HorizontalAlignment="Center"
                            Orientation="Horizontal">
                            <Rectangle
                                Width="{CompiledBinding InfoWindow.HalfLineWidth.Value}"
                                Height="1"
                                Margin="0,0,7,0"
                                Fill="{DynamicResource MainBorderColor}" />
                            <Image Source="{StaticResource PaletteImage}" />
                            <Rectangle
                                Width="{CompiledBinding InfoWindow.HalfLineWidth.Value}"
                                Height="1"
                                Margin="7,0,0,0"
                                Fill="{DynamicResource MainBorderColor}" />
                        </StackPanel>

                        <WrapPanel Margin="5,10,0,0" HorizontalAlignment="Center">

                            <!--  Color representation  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.ColorRepresentation.Value,
                                                           Mode=OneWay}" />
                                <ComboBox
                                    x:Name="ColorRepresentationBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    MinWidth="180"
                                    Margin="5,0,0,0"
                                    Padding="5,7,1,7"
                                    Classes="CStyle x"
                                    CornerRadius="2"
                                    ItemsSource="{CompiledBinding Exif.ColorRepresentations.Value}"
                                    SelectedIndex="{CompiledBinding Exif.ColorRepresentation.Value,
                                                                    Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.ColorRepresentation.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Resolution unit  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.ResolutionUnit.Value,
                                                           Mode=OneWay}" />
                                <ComboBox
                                    x:Name="ResolutionUnitBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    MinWidth="180"
                                    Margin="5,0,0,0"
                                    Padding="5,7,1,7"
                                    Background="{DynamicResource DisabledBackgroundColor}"
                                    Classes="CStyle x"
                                    CornerRadius="2"
                                    ItemsSource="{CompiledBinding Exif.ResolutionUnits.Value}"
                                    SelectedIndex="{CompiledBinding Exif.ResolutionUnit.Value,
                                                                    Mode=OneWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{Binding #ResolutionUnitBox.SelectedItem}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>

                            </StackPanel>

                            <!--  Compression  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.Compression.Value,
                                                           Mode=OneWay}" />
                                <ComboBox
                                    x:Name="CompressionBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    MinWidth="180"
                                    Margin="5,0,0,0"
                                    Padding="5,7,1,7"
                                    Classes="CStyle x"
                                    CornerRadius="2"
                                    ItemsSource="{CompiledBinding Exif.Compressions.Value}"
                                    SelectedIndex="{CompiledBinding Exif.Compression.Value,
                                                                    Mode=OneWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.Compression.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Compressed bits pixel  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.CompressedBitsPixel.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="CompressedBitsPerPixelBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Background="{DynamicResource DisabledBackgroundColor}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetCompressedBitsPixelCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    IsReadOnly="True"
                                    Text="{CompiledBinding Exif.CompressedBitsPixel.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.CompressedBitsPixel.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                        </WrapPanel>
                    </StackPanel>

                    <!--  Camera info  -->
                    <StackPanel IsEnabled="{CompiledBinding Exif.IsExifAvailable.Value}">
                        <StackPanel
                            Height="20"
                            Margin="15,20"
                            HorizontalAlignment="Center"
                            Orientation="Horizontal">
                            <Rectangle
                                Width="{CompiledBinding InfoWindow.HalfLineWidth.Value}"
                                Height="1"
                                Margin="0,0,7,0"
                                Fill="{DynamicResource MainBorderColor}" />
                            <Path
                                Data="{StaticResource ImageInfoGeometry}"
                                Fill="{DynamicResource MainTextColor}"
                                Stretch="Uniform" />
                            <Rectangle
                                Width="{CompiledBinding InfoWindow.HalfLineWidth.Value}"
                                Height="1"
                                Margin="7,0,0,0"
                                Fill="{DynamicResource MainBorderColor}" />
                        </StackPanel>

                        <WrapPanel Margin="5,10,0,0" HorizontalAlignment="Center">

                            <!--  Camera maker  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.CameraMaker.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="CameraMakerBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetCameraMakerCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.CameraMaker.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.CameraMaker.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Camera model  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.CameraModel.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="CameraModelBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetCameraModelCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.CameraModel.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.CameraModel.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>


                            <!--  Fnumber  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.Fnumber.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="FNumberBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetFNumberCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.FNumber.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.FNumber.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Max aperture  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.MaxAperture.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="MaxApertureBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetMaxApertureCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.MaxAperture.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.MaxAperture.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Exposure bias  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.ExposureBias.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="ExposureBiasBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetExposureBiasCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.ExposureBias.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.ExposureBias.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Exposure time  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.ExposureTime.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="ExposureTimeBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetExposureTimeCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.ExposureTime.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.ExposureTime.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>


                            <!--  Exposure program  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.ExposureProgram.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="ExposureProgramBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetExposureProgramCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.ExposureProgram.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.ExposureProgram.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Digital zoom  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{Binding Translation.DigitalZoom.Value, Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="DigitalZoomBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetDigitalZoomCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.DigitalZoom.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.DigitalZoom.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>


                            <!--  Focal length  -->

                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.FocalLength.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="FocalLengthBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetFocalLengthCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.FocalLength.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.FocalLength.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Focal length 35mm  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.FocalLength35mm,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="FocalLength35mmBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetFocalLength35MmCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.FocalLength35Mm.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.FocalLength35Mm.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>


                            <!--  ISO speed  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.ISOSpeed.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="IsoSpeedBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetISOSpeedCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.ISOSpeed.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.ISOSpeed.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Metering mode  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.MeteringMode.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="MeteringModeBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetMeteringModeCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.MeteringMode.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.MeteringMode.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>


                            <!--  Contrast  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.Contrast.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="ContrastBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetContrastCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.Contrast.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.Contrast.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Saturation  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.Saturation.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="SaturationBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetSaturationCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.Saturation.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.Saturation.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>


                            <!--  Sharpness  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.Sharpness.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="SharpnessBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetSharpnessCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.Sharpness.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.Sharpness.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  White balance  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.WhiteBalance.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="WhiteBalanceBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetWhiteBalanceCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.WhiteBalance.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.WhiteBalance.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>


                            <!--  Flash energy  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.FlashEnergy.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="FlashEnergyBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetFlashEnergyCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.FlashEnergy.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.FlashEnergy.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Flash mode  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.FlashMode.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="FlashModeBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetFlashModeCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.FlashMode.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.FlashMode.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Light source  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.LightSource.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="LightSourceBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetLightSourceCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.LightSource.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.LightSource.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Brightness  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.Brightness.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="BrightnessBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetBrightnessCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.Brightness.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.Brightness.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Photometric interpretation  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.PhotometricInterpretation.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="PhotometricInterpretationBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetPhotometricInterpretationCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.PhotometricInterpretation.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.PhotometricInterpretation.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Lens maker  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.LensMaker.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="LensMakerBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetLensMakerCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.LensMaker.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.LensMaker.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Lens model  -->
                            <StackPanel Margin="10" Orientation="Horizontal">
                                <TextBlock
                                    Width="{CompiledBinding InfoWindow.TextWidth}"
                                    Margin="0,8,2,0"
                                    Classes="txt"
                                    Text="{CompiledBinding Translation.LensModel.Value,
                                                           Mode=OneWay}" />
                                <customControls:FuncTextBox
                                    x:Name="LensModelBox"
                                    Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                    Classes=" TStyle x"
                                    Command="{CompiledBinding Exif.SetLensModelCommand}"
                                    CommandParameter="{Binding $self.Text}"
                                    Text="{CompiledBinding Exif.LensModel.Value,
                                                           Mode=TwoWay}" />
                                <customControls:CopyButton
                                    Classes="altHover BorderStyle"
                                    CopyText="{CompiledBinding Exif.LensModel.Value,
                                                               Mode=OneWay}"
                                    IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                    ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                  Mode=OneWay}">
                                    <Path Classes="CopyPath Xl" />
                                </customControls:CopyButton>
                            </StackPanel>

                            <!--  Exif version  -->
                            <StackPanel Orientation="Horizontal">
                                <StackPanel Margin="10" Orientation="Horizontal">
                                    <TextBlock
                                        Width="{CompiledBinding InfoWindow.TextWidth}"
                                        Margin="0,8,2,0"
                                        Classes="txt"
                                        Text="{CompiledBinding Translation.ExifVersion.Value,
                                                               Mode=OneWay}" />
                                    <customControls:FuncTextBox
                                        x:Name="ExifVersionBox"
                                        Width="{CompiledBinding InfoWindow.TextBoxWidth.Value}"
                                        Classes=" TStyle x"
                                        Command="{CompiledBinding Exif.SetExifVersionCommand}"
                                        CommandParameter="{Binding $self.Text}"
                                        Text="{CompiledBinding Exif.ExifVersion.Value,
                                                               Mode=TwoWay}" />
                                    <customControls:CopyButton
                                        Classes="altHover BorderStyle"
                                        CopyText="{CompiledBinding Exif.ExifVersion.Value,
                                                                   Mode=TwoWay}"
                                        IsVisible="{CompiledBinding InfoWindow.IsCopyButtonEnabled.Value}"
                                        ToolTip.Tip="{CompiledBinding Translation.Copy.Value,
                                                                      Mode=OneWay}">
                                        <Path Classes="CopyPath Xl" />
                                    </customControls:CopyButton>
                                </StackPanel>
                            </StackPanel>

                        </WrapPanel>
                    </StackPanel>
                </StackPanel>
            </Panel>
        </customControls:AutoScrollViewer>
    </Panel>
</UserControl>