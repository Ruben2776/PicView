<UserControl
    x:Class="PicView.Avalonia.Views.Config.InterfaceConfigurationView"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:customControls="clr-namespace:PicView.Avalonia.CustomControls"
    xmlns:ui="clr-namespace:PicView.Avalonia.UI"
    xmlns:viewModels="clr-namespace:PicView.Core.ViewModels;assembly=PicView.Core"
    x:DataType="viewModels:CoreViewModel">

    <StackPanel HorizontalAlignment="Center" Orientation="Vertical">

        <StackPanel Classes="searchHeader">
            <Image
                Width="18"
                Height="18"
                Source="{StaticResource ColumnsSettingsImage}" />
            <TextBlock
                Margin="0,20,0,20"
                Classes="txt bold"
                Text="{CompiledBinding Translation.InterfaceConfiguration.Value,
                                       Mode=OneWay}" />
        </StackPanel>

        <Border ui:SearchProperties.Keywords="{CompiledBinding SettingsViewModel.SearchData.ShowBottomToolbarSearchTags}" Classes="searchBorder">
            <customControls:TextIconButton
                x:Name="ShowBottomToolbarButton"
                Classes="MenuItemHover settingsTextIconButton"
                Command="{Binding MainWindows.ActiveWindow.Value.ToggleBottomToolbarCommand}"
                Icon="{StaticResource Panel-Bottom}"
                IconHeight="18"
                IconMargin="10,0,10,0"
                IconWidth="18"
                Text="{CompiledBinding Translation.IsShowingBottomToolbar.Value,
                                       Mode=OneWay}" />
        </Border>


        <Border ui:SearchProperties.Keywords="{CompiledBinding SettingsViewModel.SearchData.ShowUISearchTags}" Classes="searchBorder">
            <StackPanel>
                <customControls:TextIconButton
                    x:Name="HideUIButton"
                    Classes="MenuItemHover settingsTextIconButton"
                    Command="{CompiledBinding MainWindows.ActiveWindow.Value.ToggleInterfaceCommand}"
                    Icon="{StaticResource EyeImage}"
                    IconHeight="18"
                    IconMargin="10,0,10,0"
                    IconWidth="18"
                    IsVisible="{Binding !MainWindows.ActiveWindow.Value.IsUIShown.Value}"
                    Text="{CompiledBinding Translation.IsShowingUI.Value,
                                           Mode=OneWay}" />
                <customControls:TextIconButton
                    x:Name="ShowUIButton"
                    Classes="MenuItemHover settingsTextIconButton"
                    Command="{CompiledBinding MainWindows.ActiveWindow.Value.ToggleInterfaceCommand}"
                    Icon="{StaticResource EyeOffImage}"
                    IconHeight="18"
                    IconMargin="10,0,10,0"
                    IconWidth="18"
                    IsVisible="{Binding MainWindows.ActiveWindow.Value.IsUIShown.Value}"
                    Text="{CompiledBinding Translation.IsShowingUI.Value,
                                           Mode=OneWay}" />
            </StackPanel>
        </Border>

        <!--  Fade buttons toggle  -->
        <Border ui:SearchProperties.Keywords="{CompiledBinding SettingsViewModel.SearchData.ShowFadeInButtonsSearchTags}" Classes="searchBorder">
            <customControls:TextIconButton
                x:Name="ToggleFadeUIButton"
                Classes="MenuItemHover settingsTextIconButton"
                Command="{CompiledBinding SettingsViewModel.ToggleFadeButtonsCommand}"
                Icon="{StaticResource CursorImage}"
                IconHeight="18"
                IconMargin="10,0,10,0"
                IconWidth="18"
                Text="{CompiledBinding Translation.IsShowingFadingUIButtons.Value,
                                       Mode=OneWay}" />
        </Border>

        <!--  Show hover bar  -->
        <Border ui:SearchProperties.Keywords="{CompiledBinding SettingsViewModel.SearchData.ShowHoverNavigationBarSearchTags}" Classes="searchBorder">
            <customControls:TextIconButton
                x:Name="ToggleHoverButton"
                Classes="MenuItemHover settingsTextIconButton"
                Command="{CompiledBinding MainWindows.ActiveWindow.Value.ToggleHoverBarCommand}"
                Icon="{StaticResource CursorImage}"
                IconHeight="18"
                IconMargin="10,0,10,0"
                IconWidth="18"
                Text="{CompiledBinding Translation.IsShowingHoverNavigationBar.Value,
                                       Mode=OneWay}" />
        </Border>

    </StackPanel>
</UserControl>