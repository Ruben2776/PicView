<Window
    ExtendClientAreaTitleBarHeightHint="-1"
    ExtendClientAreaToDecorationsHint="True"
    Height="835"
    MinHeight="700"
    MinWidth="660"
    Width="975"
    x:Class="PicView.Avalonia.MacOS.Views.PrintPreviewWindow"
    x:DataType="viewModels:MainWindowViewModel"
    xmlns="https://github.com/avaloniaui"
    xmlns:main="clr-namespace:PicView.Avalonia.Views.Main;assembly=PicView.Avalonia"
    xmlns:viewModels="clr-namespace:PicView.Core.ViewModels;assembly=PicView.Core"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border
        BorderBrush="{DynamicResource WindowBorderColor}"
        BorderThickness="1"
        CornerRadius="8">
        <DockPanel>

            <DockPanel
                Background="{DynamicResource WindowTitleBackgroundColor}"
                DockPanel.Dock="Top"
                Height="28"
                PointerPressed="MoveWindow">

                <TextBlock
                    Classes="txt"
                    Text="{CompiledBinding Translation.Print.Value,
                                           Mode=OneWay}"
                    TextAlignment="Center" />
            </DockPanel>

            <main:PrintPreviewView
                Background="{DynamicResource WindowMainBackgroundColor}"
                DockPanel.Dock="Bottom"
                x:Name="PrintPreviewView" />
        </DockPanel>
    </Border>
</Window>