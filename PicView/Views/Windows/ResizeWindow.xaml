<Window x:Class="PicView.Views.Windows.ResizeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:uc="clr-namespace:PicView.Views.UserControls"
        Width="325"
        BorderBrush="{DynamicResource WindowBorderColorBrush}"
        BorderThickness="1"
        FontFamily="/PicView;component/Themes/Resources/fonts/#Roboto"
        Foreground="{StaticResource MainColorBrush}"
        ResizeMode="CanMinimize"
        SizeToContent="Height"
        Style="{DynamicResource MyWindowStyle}"
        mc:Ignorable="d">
    <DockPanel Background="{StaticResource NoisyBg}">
        <Grid
            x:Name="TitleBar"
            Height="30"
            Background="{StaticResource WindowBackgroundColorBrush}"
            DockPanel.Dock="Top">

            <uc:Logo HorizontalAlignment="Left" />

            <uc:MinButton
                x:Name="MinButton"
                Width="30"
                Height="30"
                Margin="0,0,30,0"
                HorizontalAlignment="Right"
                VerticalAlignment="Center" />

            <uc:CloseButton
                x:Name="CloseButton"
                Width="30"
                Height="30"
                HorizontalAlignment="Right"
                VerticalAlignment="Center" />
        </Grid>

        <Rectangle
            Height="1"
            DockPanel.Dock="Top"
            Fill="{StaticResource BorderBrush}" />

        <TabControl
            Padding="0"
            BorderBrush="{x:Null}"
            BorderThickness="0"
            DockPanel.Dock="Top">
            <TabControl.Resources>
                <Style TargetType="{x:Type TabControl}">
                    <Setter Property="SnapsToDevicePixels" Value="True" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type TabControl}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>

                                    <Border>
                                        <UniformGrid
                                            x:Name="HeaderPanel"
                                            IsItemsHost="True"
                                            Rows="1" />
                                    </Border>

                                    <Border
                                        x:Name="Border"
                                        Grid.Row="1"
                                        Background="{StaticResource NoisyBg}"
                                        BorderBrush="{x:Null}"
                                        BorderThickness="0">
                                        <ContentPresenter x:Name="PART_SelectedContentHost" ContentSource="SelectedContent" />
                                    </Border>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
                <Style TargetType="{x:Type TabItem}">
                    <Setter Property="FontFamily" Value="/PicView;component/Themes/Resources/fonts/#Roboto Medium" />
                    <Setter Property="Foreground" Value="{StaticResource MainColorBrush}" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type TabItem}">
                                <Grid Background="{StaticResource BackgroundSubtleHighlightBrush}">
                                    <Rectangle
                                        Width="1"
                                        Height="35"
                                        HorizontalAlignment="Left"
                                        Fill="{StaticResource BorderBrush}" />
                                    <Rectangle
                                        Name="Border"
                                        Height="2"
                                        VerticalAlignment="Bottom" />
                                    <Rectangle
                                        Height="1"
                                        VerticalAlignment="Bottom"
                                        Fill="{StaticResource BorderBrush}" />
                                    <ContentPresenter
                                        x:Name="ContentSite"
                                        Margin="0,9,0,9"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        ContentSource="Header"
                                        RecognizesAccessKey="True" />
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Panel.ZIndex" Value="100" />
                                        <Setter TargetName="Border" Property="Fill" Value="{DynamicResource ResourceKey=ChosenColorBrush}" />
                                        <Setter Property="Foreground" Value="{DynamicResource ChosenColorBrush}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
                <Style TargetType="{x:Type Grid}">
                    <Setter Property="Background" Value="{StaticResource NoisyBg}" />
                </Style>
            </TabControl.Resources>
            
            <TabItem FontSize="14" Header="{StaticResource SingleImage}">
                
            </TabItem>

            <TabItem FontSize="14" Header="{StaticResource BatchResize}">
                <Label Content="Coming soon..." HorizontalAlignment="Center" Margin="0,10,0,10" />
            </TabItem>
            
        </TabControl>
    </DockPanel>
</Window>
